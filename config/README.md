# config/

Keep your own project configs here (e.g. `config/my_study.yaml`) — this
directory is a convention, not something OmicsFusion reads automatically.

`omicsfusion init <dir>` scaffolds a starter `project.yaml`; see
`examples/demo/config.yaml` for a complete, working reference and
`schemas/project.schema.json` for the full schema.
