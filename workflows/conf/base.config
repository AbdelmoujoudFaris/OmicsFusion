// Default resource requests for all OmicsFusion processes.
// Individual processes can override via `withName:` selectors.

process {
    cpus   = 2
    memory = '4.GB'
    time   = '2.h'

    withLabel: 'ml' {
        cpus   = 4
        memory = '8.GB'
    }
}
